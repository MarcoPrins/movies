import React from 'react';
import { mount } from 'enzyme';

import Modal from '../../../components/shared/Modal';

describe('Modal', () => {
  it('has renders the correct output');
  it('calls the passed in toggle function if you click close');
});
