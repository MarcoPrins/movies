import React from 'react';
import { mount } from 'enzyme';

import MovieForm from '../../../components/movies/MovieForm';

describe('MovieForm', () => {
  it('makes a post request when you create a movie (no movie passed in)');
  it('makes a put request when you edit an existing movie (movie passed in)');
});
